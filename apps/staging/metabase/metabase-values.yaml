apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metabase
  namespace: metabase
spec:
  chart:
    spec:
      version: 5.48.0 # or latest Bitnami version
  values:
    service:
      type: ClusterIP
    persistence:
      enabled: true
      size: 1Gi 